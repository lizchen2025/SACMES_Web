# SACMES Web Application Dependencies
# Optimized for Python 3.9+ on UBI9 / OpenShift

# Web Framework
Flask>=2.3.0,<3.0.0
gunicorn>=21.0.0

# WebSocket Support with Redis Message Queue
Flask-SocketIO>=5.3.6
python-socketio>=5.10.0
# MIGRATED from eventlet to gevent for better performance and active maintenance
# gevent provides: faster event loop (libev), better CPU task scheduling, active development
# Flask-SocketIO 5.x has built-in gevent WebSocket support - no need for gevent-websocket
gevent>=24.2.0
# REMOVED gevent-websocket: unmaintained since 2017, has fragmentation bugs with messages >32KB

# Redis for session storage and message queue
redis>=5.0.0

# Scientific Computing
numpy>=1.24.0
scipy>=1.10.0

# Note: uuid is built-in to Python 3.x, no need to install